Implement two changes:

Upgrade x20 to a real visible wheel with arrow indicator, 4s ease-out spin, and up to 2 bets.
Add Crash minigame (airplane) v1 with clean state + cashout timing.
Keep frontend-only, reuse shared bet helpers + minigame stats. Correctness first, smooth but not invasive animations.
=====================================================
PART A — x20 WHEEL v2
A1) Real wheel (fully visible)
Render a circular wheel showing all 22 segments:
10 black (x2)
7 red (x3)
4 blue (x5)
1 gold (x20)
Add a fixed arrow/pointer (top) that indicates the winning segment.
Wheel rotates; arrow stays fixed.
Implementation suggestion:

Use SVG arcs for 22 segments OR conic-gradient + overlay markers. Choose the simplest working approach.
A2) Spin animation
Duration: 4000ms
Strong ease-out slow-down.
Deterministic stop: preselect winning segment index (0..21) uniformly; compute final rotation so that segment ends under the arrow.
Add several full rotations + final offset.
A3) Up to 2 bets
Player can select up to two colors (black/red/blue/gold).
Show selection count label: “0/2”, “1/2”, “2/2”.
Add buttons:
“Obstaw” (enables selection mode or simply means selection area)
“Zakręć” (starts the spin)
Prevent selecting more than 2; show inline message in Polish.
During spin: disable selection and buttons.
A4) Payout
Deduct bet once.
If result color matches ANY selected color: payout = bet * multiplier of winning color; else 0.
Stats update net profit correctly.
=====================================================
PART B — CRASH v1
B1) Add route + hub card
Add a new game card “Crash” on /minigierki and route /minigierki/crash.
B2) Crash rules (deterministic round)
Player sets bet amount and clicks “Start”.
Deduct bet immediately.
Generate a crash point once per round at start: crashAt (e.g. between 1.00 and 10.00, skewed so most crashes are 1.5–1.9 but sometimes higher).
Use a simple skewed distribution:
Example: u = Math.random()
crashAt = 1 + (1 / Math.max(0.02, u)) * 0.08 then clamp to 10
Or another approach that heavily weights 1.5–1.9 but allows rare 3x–10x.
Multiplier starts at 1.00 and increases over time (tick every ~50ms) using a smooth curve:
simple linear or mild exponential is fine.
Player can press “Cashout” any time before crash:
payout = bet * currentMultiplier (rounded to cents)
end round
If multiplier reaches crashAt first -> crash:
payout = 0
end round
B3) UI/animation
Show a simple “plane”/rocket moving upward slightly with the multiplier increasing (no heavy canvas).
Show big multiplier text (cyan) e.g. “x1.72”.
Buttons:
“Start” (disabled if round running)
“Cashout” (enabled only while running)
After round ends, show result text:
Win: “Wypłacono: #X.XX przy xM”
Lose: “Crash przy xC — przegrałeś”
Prevent double cashout and double start (guards).
B4) Stats
Each Crash round counts as minigame played; wagered += bet.
Profit net: payout - bet.
Acceptance
x20 wheel is fully visible with arrow and lands correctly after 4s.
x20 supports up to 2 bets, UI shows 0/2..2/2.
Crash round is deterministic: crash point set once, multiplier increases until crash/cashout.
No negative balance, no double execution, stats correct.